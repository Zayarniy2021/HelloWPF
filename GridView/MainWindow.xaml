<Window x:Class="GridView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GridView"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <DataGrid Margin="10" x:Name="dgEmployee" AutoGenerateColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Width="120" Binding="{Binding Name}" />
                <DataGridTextColumn  Header="Возраст" Width="50" Binding="{Binding Age}" />
                <DataGridTextColumn Header="Зарплата" Width="150" Binding="{Binding Salary}" />
            </DataGrid.Columns>            
        </DataGrid>

    </Grid>

</Window>
